# payflow(mySns) — 핵심 설계 결정 요약

> 전체 106개 커밋의 변경 의도를 분석해 도출. 각 결정은 "왜 그렇게 했나 / 무엇을 막았나" 중심.

| # | 영역 | 핵심 결정 | 무엇을 막았나 / 효과 |
|---|------|-----------|----------------------|
| 1 | 인증·인가 | 토큰을 httpOnly 쿠키로 운반 (Auth.js v5 BFF, 운반경계만 이동) | localStorage 평문 → XSS 노출 차단, SSR 토큰 접근 가능 |
| 2 | 인증·인가 | GraphQL 3-Layer 인가 (HTTP · @PreAuthorize · 필드 마스킹) | 단일 /graphql 엔드포인트라 URL 보안 무력 → 계층별 보강 |
| 3 | 인증·인가 | FE↔BE 검증 규칙을 계약으로 동기화 | 규칙 불일치, 필드별 에러를 인라인 폼 에러로 표시 |
| 4 | 인증·인가 | Refresh 시 DB 재조회로 권한 재검증 | role 강등 미반영(토큰 role만 신뢰) 차단 |
| 5 | 데이터 | 인메모리 스텁 → JPA, Store 추상화로 무중단 교체 | 도메인 굳기 전 DB 스키마 결합 회피 |
| 6 | 데이터 | 복합키는 네이티브 `INSERT ON CONFLICT` / `DELETE` | 파생 save merge no-op · 부정확한 affected 행수 |
| 7 | 데이터 | 비정규화 카운터 + `@BatchMapping` | 요청마다 `COUNT(*)`, N+1 제거 (피드 20장 → 1 SELECT) |
| 8 | 데이터 | 카운터 쿼리에 `flushAutomatically=true` | PC clear로 예약된 DELETE가 증발하는 버그 |
| 9 | 데이터 | DB-레벨 FK 무결성 (CASCADE / SET NULL) | 앱 의존 정리 누락, 탈퇴는 금융 가드 통과 후 CASCADE |
| 10 | 금융·동시성 | `WHERE balance >= amount` 원자 차감 | read-modify-write 잔액 경합 |
| 11 | 금융·동시성 | held 에스크로 패턴을 정산·펀딩에 공유 (hold/release/settleHeld) | 비관락+원자쿼리로 8번 PC 취약점 회피, all-or-nothing 미달 자동환불 |
| 12 | 이벤트·실시간 | 알림은 `AFTER_COMMIT` + `REQUIRES_NEW` | 미커밋 데이터 발행, 12h 쿨다운으로 폭주 방지 |
| 13 | 이벤트·실시간 | SSE 단일 스트림(알림+메시지) + `?token=` 인증 폴백 | 폴링(7s/30s) 제거, onopen 전체 resync로 끊김 자가복구 |
| 14 | AI·업로드 | OCR 엔진 교체(→Gemini) + 추천-only (`OcrEngine` 추상화) | 오인식의 데이터 오염, SSRF / path-traversal 차단 |
| 15 | AI·업로드 | 업로드 2단계 commit + UUID 파일명 | path traversal, 고아 파일(Cleaner 정리) |
| 16 | 인프라 | 크로스플랫폼 빌드 + 저메모리 JVM (`$BUILDPLATFORM`, RAM75%+SerialGC) | QEMU 제거(빠른 빌드), 단일 서버 자원 절약 |
| 17 | 인프라 | Nginx 뒤로 전부 은닉, Grafana는 RBAC 게이트 (auth_request→BFF ADMIN) | 포트 직접 노출 시 게이트 우회 → 금지, 관측 접근을 앱 RBAC과 일체화 |
| 18 | 인프라 | Codegen 스키마 환경 분리 (라이브 / vendored 폴백) | vendored 미동기화 시 Docker 빌드만 깨짐 → 운영 메모로 관리 |
| 19 | 인프라 | RED + 런타임 대시보드 (VM / Loki) | 골든시그널 17패널 + 런타임 7패널, Discord 알림 |
| 20 | FE 상태 | Apollo 캐시 정밀수술 + 낙관적 UI (optimistic, writeFragment) | 서버 true일 때만 evict → "삭제됐다 새로고침하면 부활" illusion 차단 |
